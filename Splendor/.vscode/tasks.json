{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Copy UI resources",
      "type": "shell",
      "command": "bash",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "args": [
        "-lc",
        "set -e; for d in classes out; do rm -rf \"$d/UI/images\" \"$d/UI/views\" \"$d/UI/styles\"; mkdir -p \"$d/UI\"; cp -a src/UI/images \"$d/UI/\"; cp -a src/UI/views \"$d/UI/\"; if [ -d src/UI/styles ]; then cp -a src/UI/styles \"$d/UI/\"; fi; done"
      ],
      "problemMatcher": [],
      "presentation": {
        "reveal": "never",
        "panel": "shared"
      }
    }
  ]
}